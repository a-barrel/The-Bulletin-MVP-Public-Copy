# Runtime mode defaults to "offline" locally and "online" in production.
# Uncomment to override.
# PINPOINT_RUNTIME_MODE=online

# Firebase service account credentials (JSON format)
# Required for production deployments. Leave blank when using the Firebase emulator.
FIREBASE_SERVICE_ACCOUNT_JSON=

# MongoDB connection URIs
# Used when PINPOINT_RUNTIME_MODE=online (Render, etc.).
MONGODB_URI=
# Used when PINPOINT_RUNTIME_MODE=offline or when no mode is specified locally.
MONGODB_URI_OFFLINE=mongodb://127.0.0.1:27017/pinpoint

# Demo bearer token accepted while running offline.
PINPOINT_OFFLINE_DEMO_TOKEN=demo-token

# Optional: explicitly set the Firebase Auth emulator endpoint.
# FIREBASE_AUTH_EMULATOR_HOST=127.0.0.1:9099

# Server port
PORT=5000
